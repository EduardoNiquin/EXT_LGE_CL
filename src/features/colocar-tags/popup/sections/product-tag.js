export function render(container) {
  container.innerHTML = `
    <div class="ct-view">
      <div class="ct-state">
        <p>Sección <strong>Tag de Producto</strong> — pendiente.</p>
        <p class="ct-state-hint">El flujo va a ser similar al de Delivery pero apuntando a la fila de Product Tag 1/2. Cuando definamos los pasos, lo armamos acá.</p>
      </div>
    </div>
  `;
}
