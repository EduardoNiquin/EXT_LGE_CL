import { sendMessage } from '../shared/messaging/messaging.js';

document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  app.textContent = 'EXT LGE CL';
});
